fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
aiosqlite==0.20.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
pyyaml==6.0.2
websockets==13.0

# Document text extraction (optional - tools detect availability at runtime)
python-docx>=1.1.2
openpyxl>=3.1.5
python-pptx>=1.0.2
pypdf>=5.1.0
pdfminer.six>=20240706
pytesseract>=0.3.13
Pillow>=10.0.0

# Security features (optional - tools detect availability at runtime)
cryptography>=41.0.0

# System integration
psutil>=5.9.0
watchdog>=4.0.0

# Browser / web integration (optional - tools detect availability at runtime)
beautifulsoup4>=4.12.0

# Phase 2: Desktop integration (system packages, not pip)
# Install with: sudo apt install xclip scrot wmctrl xdotool libnotify-bin x11-xserver-utils
# All desktop_tools.py capabilities gracefully degrade if these are not installed.

# Phase 3: Voice & Audio Intelligence (all optional — degrade gracefully)
# --- Speech-to-Text (choose one) ---
# faster-whisper is recommended: smaller memory footprint, int8 quantized
faster-whisper>=1.0.0
# OR: pip install openai-whisper (heavier, requires torch)

# --- Text-to-Speech ---
pyttsx3>=2.90
# System fallback: sudo apt install espeak-ng festival

# --- Audio metadata ---
mutagen>=1.47.0
# System fallback: sudo apt install ffmpeg  (provides ffprobe)
